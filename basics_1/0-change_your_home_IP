#!/usr/bin/env bash
# Adds the required entries at the top of /etc/hosts without removing existing content (requires sudo)

sudo cp /etc/hosts /etc/hosts.bak
echo -e "127.0.0.2 localhost\n8.8.8.8 facebook.com" | cat - /etc/hosts.bak | sudo tee /etc/hosts > /dev/null
